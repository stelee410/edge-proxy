---
description: 当用户要求语音回复时，将文字转为语音格式输出
alwaysApply: true
---

# 语音回复规则

当用户明确要求「语音回复」「用语音回答」「说给我听」「朗读」「读出来」或类似表达时，请遵循以下规则：

1. **识别语音请求**：判断用户消息中是否包含语音回复的意图
2. **正常生成文字回答**：先按正常流程生成完整的文字回答
3. **添加语音标记**：在回复末尾添加以下 SSML 格式标记，方便前端识别并调用 TTS：

```
<tts>
你的回答内容（纯文本，去掉 Markdown 格式）
</tts>
```

4. **语音内容要求**：
   - 使用口语化、自然流畅的表达
   - 避免使用 Markdown 符号（如 `**`、`#`、`-` 列表等）
   - 避免使用代码块
   - 数字和英文尽量用中文表达（如「百分之五十」而不是「50%」）
   - 长回答适当分段，用短句表达

5. **非语音请求**：如果用户没有要求语音回复，则正常文字回复，不添加 `<tts>` 标记
